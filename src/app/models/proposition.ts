export class PropositionModel {
  id: number;
  name: string;
  createDate: string;
  authorId: number;
  description: string;
  status: number;
  location: [number, number];
  images: string;
  authorName: string;
  authorEmail: string;
  authorPhoneNumber: string;
  // authorPhoneNumber: string;
}
